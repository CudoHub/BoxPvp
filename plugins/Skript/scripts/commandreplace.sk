command /badge:
    trigger:
        execute player command "tags"

command /badges:
    trigger:
        execute player command "tags"

command /fairysoul:
    trigger:
        execute player command "hb me"

command /fairysouls:
    trigger:
        execute player command "hb me"